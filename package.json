{
  "name": "json_mock_server",
  "version": "1.0.0",
  "description": "json_server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ravi",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
