{
  "name": "json_mock_server",
  "version": "1.0.0",
  "description": "json_server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ravi",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
